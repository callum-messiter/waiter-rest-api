define({
  "name": "Waiter REST API",
  "version": "1.0.0",
  "description": "The RESTful API for waiter, serving all client applications.",
  "url": "http://localhost:3000",
  "sampleUrl": "/api",
  "title": "Waitr API V1.0",
  "template": {
    "forceLanguage": "en"
  },
  "header": {
    "title": "Getting Started",
    "content": "<hr>\n<h1>Authentication</h1>\n<p>The Waitr REST API uses the OAuth 2.0 protocol to authorize calls. OAuth is an open standard that many companies use to provide secure access to protected resources.</p>\n<p>After a user successfully logs in, the <code>token</code> field in the Login response contains an authentication token:</p>\n<pre><code>{  \n    &quot;user&quot;: {  \n        &quot;userId&quot;: String,\n        &quot;firstName&quot;: String,\n        &quot;lastName&quot;: String,\n        &quot;email&quot;: String,\n        &quot;role&quot;: Int,\n        &quot;token&quot;: String\n    },\n    ...\n}\n</code></pre>\n<p>You must include this token in API requests in the Authorization header.</p>\n<p>This sample request uses a bearer token to list user details:</p>\n<pre><code>curl -X GET http://localhost:3000/restaurant/:restaurantId \\\n  -H &quot;Content-Type:application/json&quot; \\\n  -H &quot;Authorization: {token}&quot;\n</code></pre>\n<p>Soon the API will be updated to comply fully with the OAuth 2.0 protocol, with the use of access and refresh tokens.</p>\n<p>.</p>\n<h1>API Requests</h1>\n<p>To construct a REST API request, combine these components:</p>\n<ul>\n<li>\n<p>The HTTP method:</p>\n<ul>\n<li>\n<p><code>GET</code> Requests data from a resource.</p>\n</li>\n<li>\n<p><code>POST</code> Submits data to a resource to process.</p>\n</li>\n<li>\n<p><code>PUT</code> Updates a resource.</p>\n</li>\n<li>\n<p><code>PATCH</code> Partially updates a resource.</p>\n</li>\n<li>\n<p><code>DELETE</code> Deletes a resource.</p>\n</li>\n</ul>\n</li>\n<li>\n<p>The URL to the API service: <code>http://api.waitr.live</code></p>\n</li>\n<li>\n<p>The URI to the resource: <code>/restaurant</code></p>\n</li>\n<li>\n<p>Query parameters: Optional. Controls which data appears in the response. Use to filter, limit the size of, and sort the data in an API response.</p>\n</li>\n<li>\n<p>HTTP request headers: Includes the Authorization header with the authentication token.</p>\n</li>\n<li>\n<p>A JSON request body: Required for most GET, POST, PUT, and PATCH calls.</p>\n</li>\n</ul>\n<p>This is a simple request to update a user's first name:</p>\n<pre><code>curl -X PATCH http://localhost/user/{userId} \\\n  -H &quot;Content-Type:application/json&quot; \\\n  -H &quot;Authorization: {token}&quot; \\\n  -d '{\n    &quot;firstName&quot;: &quot;John&quot;\n  }'\n</code></pre>\n<h2>Query Parameters</h2>\n<p>For some REST GET calls, you can specify one or more optional query parameters on the request URI to filter, limit the size of, and sort the data in an API response. For filter parameters, see the individual GET calls.</p>\n<p>For example, the Order List API returns only orders that have not yet been resolved:</p>\n<pre><code>curl -X GET http://localhost:3000/order/:ownerId?liveOnly=true \\\n  -H &quot;Content-Type:application/json&quot; \\\n  -H &quot;Authorization: {token}&quot; \\\n</code></pre>\n<p>.</p>\n<h1>API Responses</h1>\n<p>Waitr API calls return HTTP status codes. Some API calls also return JSON response bodies that include information about the resource.</p>\n<h2>HTTP status codes</h2>\n<p>Each REST API request returns a success or error HTTP status code.</p>\n<h3>Success</h3>\n<p>In the responses, the API returns these HTTP status codes for successful requests:</p>\n<ul>\n<li>\n<p><code>200 OK</code>: The request succeeded.</p>\n</li>\n<li>\n<p><code>201 Created</code>: A <code>POST</code> method successfully created a resource. If the resource was already created by a previous execution of the same method, for example, the server returns the HTTP <code>200 OK</code> status code.</p>\n</li>\n<li>\n<p><code>202 Accepted</code>: The server accepted the request and will execute it later..</p>\n</li>\n<li>\n<p><code>204 No Content</code>: The server successfully executed the method but returns no response body.</p>\n</li>\n</ul>\n<h3>Error</h3>\n<p>In the responses for failed requests, the API returns HTTP <code>4XX</code> or <code>5XX</code> status codes.</p>\n<p>For all errors, the API returns an error response body that includes additional error details in this format:</p>\n<pre><code>{\n    &quot;statusCode&quot;: 404,\n    &quot;errorKey&quot;: &quot;restaurantNotFound&quot;,\n    &quot;type&quot;: &quot;_restaurant&quot;,\n    &quot;devMsg&quot;: String,\n    &quot;userMsg&quot;: String\n}\n</code></pre>\n<p>There are a number of errors that can be returned by any API endpoint, and thus they will not be specified in the individual documentation blocks, but rather here:</p>\n<pre><code>{\n    &quot;statusCode&quot;: 400,\n    &quot;errorKey&quot;: &quot;missingRequiredParams&quot;,\n    &quot;type&quot;: &quot;_auth&quot;,\n    &quot;devMsg&quot;: String,\n    &quot;userMsg&quot;: String\n}\n</code></pre>\n<pre><code>{\n    &quot;statusCode&quot;: 400,\n    &quot;errorKey&quot;: &quot;missingRequiredHeaders&quot;,\n    &quot;type&quot;: &quot;_auth&quot;,\n    &quot;devMsg&quot;: String,\n    &quot;userMsg&quot;: String\n}\n\n</code></pre>\n<pre><code>{\n    &quot;statusCode&quot;: 403,\n    &quot;errorKey&quot;: &quot;insufficientRolePrivileges&quot;,\n    &quot;type&quot;: &quot;_auth&quot;,\n    &quot;devMsg&quot;: String,\n    &quot;userMsg&quot;: String\n}\n</code></pre>\n<pre><code>{\n    &quot;statusCode&quot;: 403,\n    &quot;errorKey&quot;: &quot;insufficientPermissions&quot;,\n    &quot;type&quot;: &quot;_auth&quot;,\n    &quot;devMsg&quot;: String,\n    &quot;userMsg&quot;: String\n}\n</code></pre>\n<pre><code>{\n    &quot;statusCode&quot;: 500,\n    &quot;errorKey&quot;: &quot;internalServerError&quot;,\n    &quot;type&quot;: &quot;_unhandled&quot;,\n    &quot;devMsg&quot;: String,\n    &quot;userMsg&quot;: String\n}\n</code></pre>\n<p>In the responses, the API returns these HTTP status codes for failed requests:</p>\n<ul>\n<li>\n<p><code>400 Bad Request</code>: The server could not understand the request.</p>\n</li>\n<li>\n<p><code>401 Unauthorized</code>: The request requires authentication and the caller did not provide valid credentials.</p>\n</li>\n<li>\n<p><code>403 Forbidden</code>: The client is not authorized to access this resource although it might have valid credentials</p>\n</li>\n<li>\n<p><code>404 Not Found</code>: The server did not find anything that matches the request URI. Either the URI is incorrect or the resource is not available.</p>\n</li>\n<li>\n<p><code>405 Method Not Allowed</code>: The service does not support the requested HTTP method. For example, <code>PATCH</code>.</p>\n</li>\n<li>\n<p><code>500 Internal Server Error</code>: A system or application error occurred. Although the client appears to provide a correct request, something unexpected occurred on the server.</p>\n</li>\n<li>\n<p><code>503 Service Unavailable</code>: The server cannot handle the request for a service due to temporary maintenance.</p>\n</li>\n</ul>\n"
  },
  "defaultVersion": "0.0.0",
  "apidoc": "0.3.0",
  "generator": {
    "name": "apidoc",
    "time": "2018-07-30T11:26:24.272Z",
    "url": "http://apidocjs.com",
    "version": "0.17.6"
  }
});
